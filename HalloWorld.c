/*Hallo Wrld*/
/*Ok so this is fun. this # is called an octothorpe it comes w/ spec commands
that is needed before it go throu a compiler phase*/
#include<stdio.h>//include is like a preproccessor that pull other files(idk)
//this <studio.h> is an header file( .h) gives it away. means Standard input/output

int main(void)//this starts a exacutable code
{
    print("Hallo, World.\n");//actually do wrk

    return 0;
}